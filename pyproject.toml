[project]
name = "log-anomaly-detection-poc"
version = "0.1.0"
description = "AIが生ログ・実データに一切触れず、メタデータのみで異常の絞り込みを行うことを実装レベルで検証するPoC"
authors = [
    { name = "Yunin", email = "yuninakamura2023@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.5",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "mypy",
    "vulture",
    "pip-audit",
    "pre-commit",
    "pandas-stubs>=3.0.5.260730",
]

[tool.ruff.lint]
select = ["E", "F", "W", "C90", "PLR", "N", "B", "ANN"]

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.lint.pylint]
max-args = 6
max-branches = 12

[tool.mypy]
strict = true
warn_unreachable = true
disallow_untyped_defs = true
# tests/type_fixtures/ は意図的に型エラーを含むfixture(Step3の型分離が
# 崩れていないことをtest_type_separation.pyがmypyのsubprocess実行で検証する
# ためのもの)。通常のCI用mypy実行(mypy src tests)からは除外する。
exclude = ["tests/type_fixtures/"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.coverage.run]
source = ["src"]
